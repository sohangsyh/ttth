Optional: self-host the Draco decoder here instead of using the default
Google-hosted CDN (see DRACO_DECODER_PATH in src/scene/models/ModelLoader.tsx).
Copy the contents of node_modules/three/examples/jsm/libs/draco/ into this
folder, then update DRACO_DECODER_PATH to '/draco/'.
