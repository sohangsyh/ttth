Optional: place `heartbeat-loop.mp3` here (see src/audio/HeartbeatAudio.tsx's
default `audioUrl`). If absent, the app falls back to a procedurally
synthesized heartbeat automatically — this file is a nice-to-have, not
required.
